workflows:
build_flutter_apk:
name: Build Flutter APK

environment:
  flutter: stable

scripts:
  - flutter --version
  - flutter pub get
  - flutter clean
  - flutter build apk --release

artifacts:
  - build/app/outputs/flutter-apk/*.apk
