workflows:
  build_android_only:
    name: Build Android APK Only

    environment:
      flutter: stable

    scripts:
      - flutter --version
      - flutter pub get
      - flutter clean
      - flutter build apk --release

    artifacts:
      - build/app/outputs/flutter-apk/*.apk
